{
  "name": "league-champion-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local champion pool and shared notebook for your League accounts.",
  "scripts": { "start": "node server.mjs", "test": "node --test tests/*.test.mjs", "catalog": "node scripts/bundle-catalog.mjs" },
  "engines": { "node": ">=22" }
}
